name: Deploy to Pages Branch

on:
  push:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Minimal test
        run: echo "minimal deploy workflow ok"
